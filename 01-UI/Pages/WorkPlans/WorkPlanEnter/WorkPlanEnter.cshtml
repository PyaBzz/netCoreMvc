@using System.Linq;
@using myCoreMvc.UI.Controllers;
@using Py.CoreMvc

@model WorkPlanEnterController.EnterModel
@{ ViewBag.Title = "WorkPlan Details"; }

<h2 id="message">@Model.Message</h2>

<a asp-controller="@(Short<WorkPlanListController>.Name)" asp-action="@nameof(WorkPlanListController.Index)">Back</a>

@if (Model.Id == Guid.Empty)
{<h3>Add WorkPlan</h3>}
else
{<h3>Edit WorkPlan</h3>}

<form method="post">
    <input type="hidden" name="Id" value="@Model.Id" />
    <label asp-for=@Model.Name></label>
    <input type="text" name="Name" value="@Model.Name" /><span asp-validation-for="Name"></span>
    <br />
    <br />
    <input type="submit" name="Save" value="Save" />
</form>